main.c
    - handle build in [exit, cd, export, unset]
    with no pipe, it should run on parent,
    should make new algorithm to fix it

run_redir.c
    - parse variable before use filename

run_cmd.c
    - parse variable before use argv

parser.c
    - handle heredoc on parse_redir()

build in

handle exit code

handle signel

handle shell level
